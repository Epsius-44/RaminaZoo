{% extends 'base.html.twig' %}

{% block title %}Supprimer un espace{% endblock %}

{% block body %}
    <div class="container">
        <h1>Êtes-vous sûr de vouloir supprimer l'espace <i>{{ espace.nom }}</i></h1>
        {{ form(formulaire) }}
        <a href="{{ path("app_espace_home")}}" class="btn btn-secondary">Annuler</a>
    </div>
{% endblock %}
