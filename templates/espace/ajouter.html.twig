{% extends 'base.html.twig' %}

{% block title %}Ajouter un espace{% endblock %}

{% block body %}

{% for message in app.flashes('error') %}
    <div class="alert alert-danger">
        {{ message }}
    </div>
{% endfor %}

<div class="container">
    <h2>Ajouter un espace</h2>
    <div class="container" style="width: 25rem; margin: 1rem;">
        {{ form(formulaire) }}
    </div>
</div>
    
{% endblock %}